# LLM Agent — Task 1

## Описание
Консольный агент, который отвечает на вопросы через Qwen Code API.

## Провайдер
- **API:** Qwen Code (развёрнут на VM)
- **Модель:** `coder-model`
- **Доступ:** через OpenAI-совместимый интерфейс

## Установка и запуск
1. Скопировать файл с переменными:
   ```bash
   cp .env.agent.example .env.agent.secret
## Task 2: Инструменты документационного агента

- **list_files** — показывает список файлов в папке wiki/
- **read_file** — читает содержимое файла

Агент сам решает, когда вызывать инструменты. Цикл до 10 шагов.

### Примеры запросов
- "How do you resolve a merge conflict?" — прочитает git-workflow.md
- "What files are in the wiki?" — покажет список файлов
## Task 3: Системный агент с query_api

### Новый инструмент: query_api
В Task 3 я добавил инструмент `query_api`, который позволяет агенту общаться с бэкендом.

**Параметры:**
- `method` — HTTP метод (GET, POST)
- `path` — путь к эндпоинту (например, `/items/`)
- `body` — тело запроса (опционально, для POST)

**Аутентификация:**
- Используется ключ `LMS_API_KEY` из файла `.env.docker.secret`
- Базовый URL бэкенда берётся из `AGENT_API_BASE_URL` (по умолчанию `http://localhost:42002`)

### Как агент выбирает инструменты
В системном промпте я объяснил агенту правила:
- **Вопросы по документации** (как что-то сделать) → `list_files` / `read_file` из wiki
- **Вопросы о данных** (сколько предметов, статус) → `query_api`
- **Вопросы о коде** (как реализована функция) → `read_file` для `.py` файлов

### Результаты benchmark
Запустил `run_eval.py` и получил следующие результаты:
- [ ] Все 10 вопросов прошли (или укажи, сколько прошло)
- [ ] Основные проблемы были с... (опиши, если были)

### Уроки, которые я извлекла
1. Важно чётко описывать в промпте, когда какой инструмент использовать
2. Нужно проверять, что все ключи берутся из переменных окружения, а не хардкодятся
3. Для отладки полезно смотреть `tool_calls` в выводе агента

### Итоговая архитектура
Агент теперь умеет:
- Читать файлы из wiki (как в Task 2)
- Вызывать API бэкенда (новое в Task 3)
- Принимать решение, какой инструмент использовать
- Возвращать ответ с `answer`, `source` (если есть) и `tool_calls`

### Запуск
```bash
uv run agent.py "How many items are in the database?"
uv run run_eval.py
